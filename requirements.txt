# Core
torch>=2.0.0
torchvision>=0.15.0
Pillow>=9.0.0
transformers>=4.35.0
accelerate>=0.24.0

# Audio synthesis — Coqui TTS does not support Python 3.12+
# edge-tts uses Microsoft neural voices (high quality, free, requires internet)
edge-tts>=6.1.9

# Fallback TTS (offline, lower quality)
pyttsx3>=2.90

# Fine-tuning
kaggle>=1.5.16
tqdm>=4.65.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
