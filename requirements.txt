# ---------------------------------------------------------------
# Setup instructions:
#   conda create -n food_nn python=3.11
#   conda activate food_nn
#   conda install pytorch torchvision pytorch-cuda=12.8 -c pytorch -c nvidia
#   pip install -r requirements.txt
# ---------------------------------------------------------------

# Vision-Language model (BLIP — used only for pseudo-label generation)
transformers>=4.40.0
accelerate>=0.30.0

# Image processing
Pillow>=10.0.0

# Audio synthesis
edge-tts>=6.1.9
pyttsx3>=2.90

# Utilities
tqdm>=4.65.0
requests>=2.31.0
